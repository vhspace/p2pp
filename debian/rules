#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	python3 setup.py build

override_dh_auto_install:
	python3 setup.py install --root=debian/p2pp --install-layout=deb

override_dh_auto_clean:
	python3 setup.py clean -a
	rm -rf build/ *.egg-info